import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  videoCard: {
    //width: 300,
    height: 300,
    marginTop: 30,
    alignContent: 'center',
    alignItems: 'center',
  },
  video: {width: 300, height: 300},
});
